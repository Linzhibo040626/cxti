# Bilibili 视频推广模板：我用 Codex + GitHub + Cloudflare + 腾讯云做了 CSTI 抽象人格测试

## 视频定位

- 平台：Bilibili
- 时长建议：60-90 秒
- 画面比例：优先 9:16 竖屏，也可裁成 16:9 横屏
- 风格：轻松、抽象、技术感、带一点“我真做出来了”的爽感
- 目标：推广 CSTI 小网页，让观众点开测试并分享结果

## 标题备选

1. 我用 Codex 做了一个抽象人格测试网页，结果有点离谱
2. 花了一晚上，用 AI + GitHub + Cloudflare 搭了个 CSTI 抽象人格测试
3. 年轻人的 MBTI 已经不够用了，来测测你是哪位抽象名人
4. 我做了个 CSTI：测出你的互联网抽象人格
5. 用 Codex 从 0 到上线，我做了个能在手机打开的人格测试网页

## 封面文案

```text
CSTI 抽象人格测试
你是哪位抽象名人？
```

小字：

```text
Codex + GitHub + Cloudflare + 腾讯云
从 0 到上线
```

## 视频结构

### 0-5 秒：开场钩子

画面：

- 手机打开 `https://csti.site`
- 快速滑过首页和几道题
- 闪一下结果页

配音：

```text
朋友们，我做了一个比 MBTI 更抽象的东西，叫 CSTI。
它不测你是 I 人还是 E 人，它直接测你像哪位互联网抽象名人。
```

字幕：

```text
CSTI 抽象人格测试
不测人格字母，直接出抽象名人
```

### 5-15 秒：介绍网站

画面：

- 展示首页标题
- 点击“开始测试”
- 显示题目卡片和 AB/CD 选项

配音：

```text
这个网页是手机竖屏设计，微信里也能直接点开。
题目一共 15 道，都是一些很年轻人精神状态的抽象场景。
比如群聊降临、赛博早八、外卖玄学、抽象会议。
```

字幕：

```text
15 道题
手机竖屏
微信可打开
抽象但认真
```

### 15-30 秒：展示题目趣味

画面：

- 连续展示 3-4 道题
- 每题停 2 秒
- 鼠标或手指选择几个选项

配音：

```text
我不想做那种一眼就能猜出规律的测试，
所以每道题的选项都会打乱。
而且结果不是简单算几个指数，
而是每个名人都有一套完整的答题组。
你怎么选，最后就会匹配到最接近的那个人。
```

字幕：

```text
选项会随机打乱
不是简单指数匹配
每个结果都有专属答题组
```

### 30-45 秒：展示结果池

画面：

- 快速切换几个结果页，或者用文字飞入展示名字

屏幕文字：

```text
可能结果：
徐静雨 / 峰哥 / 张雪峰 / 战鹰
何炅 / 黄磊 / 李诞 / 王建国
陈泽 / 马云 / 马化腾 / 柯洁
Bin哥 / JackeyLove / 韦世豪 / 周琦 ...
```

配音：

```text
结果池里有三十多个可能结果。
有体育圈，有游戏圈，有互联网人物，也有综艺和抽象文化里的名场面人物。
主打一个：测完不是很科学，但很想发给朋友。
```

字幕：

```text
30+ 抽象名人结果
娱乐向测试
适合发给朋友互相迫害
```

### 45-60 秒：制作流程

画面：

- 快速展示 Codex 对话
- GitHub 仓库页面
- Cloudflare 部署页面
- 腾讯云域名 `csti.site`

配音：

```text
整个项目我是用 Codex 辅助写代码，
然后推到 GitHub 开源，
再用 Cloudflare 部署静态网页，
最后用腾讯云买了域名 csti.site。
从想法、代码、部署到域名上线，基本就是这一套流程。
```

字幕：

```text
Codex 写代码
GitHub 开源
Cloudflare 部署
腾讯云购买域名
csti.site 上线
```

### 60-75 秒：引导访问

画面：

- 手机完整展示一次：首页 -> 答题 -> 结果 -> 复制结果
- 画面停在网址

配音：

```text
现在网站已经上线了。
你可以直接打开 csti.site，测一下自己是哪种抽象人格。
测完可以把结果发评论区，我想看看大家到底都是什么精神状态。
```

字幕：

```text
网址：csti.site
测完把结果发评论区
```

### 75-90 秒：结尾互动

画面：

- 结果页 + GitHub 仓库页面

配音：

```text
如果你也想看我怎么从 0 做这个网页，
我后面可以单独出一期教程。
包括 Codex 怎么用、GitHub 怎么推、Cloudflare 怎么部署、域名怎么绑定。
先去测，看看你是不是那个最抽象的人。
```

字幕：

```text
想看完整教程可以留言
先去 csti.site 测一下
```

## 简短版口播稿

```text
朋友们，我做了一个抽象人格测试网页，叫 CSTI。

它不是那种传统 MBTI，而是直接测你像哪位互联网抽象名人。
题目一共 15 道，都是年轻人比较熟悉的精神状态场景，比如群聊降临、赛博早八、外卖玄学、抽象会议。

为了不让大家一眼看出规律，我还做了选项乱序。
结果也不是简单用几个指数硬算，而是给每个名人设计了一套完整的答题组。

这个网页我是用 Codex 写代码，GitHub 开源，Cloudflare 部署，最后在腾讯云买了域名。
现在已经可以直接打开：csti.site。

大家可以去测一下，看看自己到底是徐静雨、张雪峰、战鹰、李诞、王建国，还是更离谱的结果。
测完把结果发评论区，我想看看 B 站到底哪种抽象人格最多。
```

## 拍摄素材清单

- 手机录屏：打开 `https://csti.site`
- 手机录屏：首页、答题过程、结果页、复制结果按钮
- 电脑录屏：Codex 写代码界面
- 电脑录屏：GitHub 仓库页面
- 电脑录屏：Cloudflare Pages 部署页面
- 电脑录屏：腾讯云域名页面，注意隐藏隐私信息
- 可选：你本人出镜 3-5 秒做开场或结尾

## 剪辑节奏建议

- 前 3 秒必须出现网页和“你是哪位抽象名人”
- 每 5-8 秒切一次画面
- 题目展示不要太久，重点是让观众觉得“有意思，想测”
- 结果名字可以快速飞入，制造信息密度
- 最后 5 秒固定露出网址 `csti.site`

## B 站简介模板

```text
我用 Codex + GitHub + Cloudflare + 腾讯云做了一个 CSTI 抽象人格测试网页。

网站：csti.site
GitHub：https://github.com/Linzhibo040626/cxti

娱乐向测试，不是严肃心理学，只负责抽象和好玩。
测完欢迎把结果发评论区。
```

## 标签建议

```text
AI编程
Codex
GitHub
Cloudflare
腾讯云
网页制作
人格测试
MBTI
抽象
整活
```
